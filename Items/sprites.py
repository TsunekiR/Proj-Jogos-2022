import pygame

key = pygame.transform.scale(
  pygame.image.load('sprites/items/key.png'),
  (30, 30)
)

lock = pygame.transform.scale(
  pygame.image.load('sprites/items/lock.png'),
  (30, 30)
)

potion = pygame.transform.scale(
  pygame.image.load('sprites/items/potion.png'),
  (30, 30)
)